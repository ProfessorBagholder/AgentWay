# Agent handoff implementation plan

Status: **Common pull handoff is the supported product mode; automatic native delivery remains an unverified future mode**. Last reviewed 2026-09-24 UTC. This is the durable work plan across context compactions and future Codex tasks. Read it with [the receiver architecture](handoff-delivery-architecture.md), [automatic receiver contract](handoff-receiver-contract.md), [conformance runbook](handoff-conformance-v1.md), [delivery gate](handoff-delivery-gate.md), [prior art](handoff-prior-art.md), [UI system](ui-system.md), and the repository's `AGENTS.md`. Update the checkpoint after each phase or changed feasibility result. This document is not evidence of implemented behavior.

2026-09-24 transport checkpoint: branch `feat/agent-task-sse` implements a shared authenticated [SSE task hint stream](handoff-task-stream.md), responding to Muse's outbound-subscription proposal. The stream filters by connection, resumes from the existing durable event journal, rechecks credentials, and passed isolated localhost HTTP replay/result tests. Guidance moves to version 19 but truthfully retains the pull-inbox contract. The branch is deployed on the test instance; public unauthenticated non-browser SSE returned 401. A harmless Codex → Muse task was claimed within 17 seconds, completed within 22 seconds and acknowledged by the sender. The temporary directed grant was removed. Muse's supplied run history reports an SSE connection before task creation, event delivery to its hook at creation, worker completion about 26 seconds later, and a disabled scheduled fallback. That supports this one SSE-triggered Muse round trip; it does not establish reliable native wake and idle return across all products. A live socket alone is insufficient for a verified receiver binding or `automatic_delivery`. Check the actual branch and PR state before resuming; do not infer merge from this checkpoint.

## Resume checkpoint

- 2026-09-24 after PR25: [PR #25](https://github.com/ProfessorBagholder/AgentWay/pull/25) merged at `db0661c` after both CI runs passed. Main was rebuilt and app/media/public agent ingress checks passed. Branch `feat/handoff-candidate-probe` adds an owner-triggered, one-shot test for an exact harmless queued pull task and a durable transport outcome record. It deliberately leaves candidate bindings unverified and automatic delivery unavailable. The migration ran on the feature-branch test instance; 72 Rust unit tests, four API tests, strict Clippy, web build/format and 17 mocked browser tests passed, with 390px dark and 900px light visual checks. The branch deployment passed local app/media health and public unauthenticated agent-ingress checks. Present a focused PR after diff review. A live probe still requires the owner to supply the intended Bot's routine URL/key through the local setup page and an authenticated sender to queue the exact test task. Native run history and sender receipt must be inspected separately. Do not promote a successful webhook POST to automatic readiness.

- 2026-09-24 after PR24: [PR #24](https://github.com/ProfessorBagholder/AgentWay/pull/24) merged at `e4e6951` after both CI runs passed. Main was rebuilt; app, media, tunnel and public authenticated agent ingress checks passed. Branch `feat/handoff-receiver-setup` adds an owner-local Grok receiver form, a configured-status bit and focused browser coverage so the URL/key can be entered without a terminal prompt or appearing in chat. This is setup for a **candidate**, not automatic readiness. Validate responsive light/dark UI, full checks and test deployment before PR review. The next blocker is a safe live proof path that can send a fixed harmless outbox delivery to an unverified candidate without marking it generally ready or bypassing the same agent's claim and sender-return checks.
- 2026-09-24 after PR23: [PR #23](https://github.com/ProfessorBagholder/AgentWay/pull/23) merged at `36c3142` after both CI runs passed. Main was rebuilt and the app, media, tunnel, public authenticated agent endpoint and protected receiver route checks passed. Branch `feat/grok-receiver-adapter` adds a dormant Grok Bot webhook transport, encrypted owner enrollment, bounded dispatch and provider-response classification. No connection is native-verified and no live automatic delivery is enabled. Run full checks and test deployment before PR review. The next gate is an owner-friendly credential setup and a harmless native idle-to-idle proof with exact recipient and original sender, followed by restart/revocation tests. Do not infer acceptance from HTTP 200.
- 2026-09-24 after PR22: [PR #22](https://github.com/ProfessorBagholder/AgentWay/pull/22) merged at `4dea3c2` after both CI runs passed. Main was rebuilt and the app, media and public authenticated agent ingress checks passed. Branch `feat/handoff-receiver-stream` adds a receiver-scoped SSE hint stream for the dormant automatic outbox. Its three isolated Rust tests cover auth isolation, replay, token rotation and restart; local validation passed 68 Rust unit tests, four API tests, strict Clippy and web formatting/build. This is a common transport option for product adapters, **not** native wake or a verified binding. Finish full checks and present a focused PR; no production binding or automatic-ready label is allowed from the stream alone. The next gated work is an owner-enrolled native adapter and a two-way idle proof for the exact target and original sender.
- 2026-09-24 after PR20: [PR #20](https://github.com/ProfessorBagholder/AgentWay/pull/20) merged at `93ef0bd` after both CI runs passed. The test instance was rebuilt from merged `main`; app, media and public authenticated agent ingress checks passed. Branch `feat/handoff-delivery-attempts` adds migration `0023`, a dormant three-attempt transport coordinator, safe owner attempt history, and five fake-adapter/restart tests. Local validation passed: 65 Rust unit tests, four API tests, strict Clippy, Rust/web formatting and web build. It has **no production adapter loop** and cannot make any real connection automatic-ready. Present the focused PR and its CI result before merging. The next gated slice is an owner-enrolled product adapter with supported native target verification and an observed two-way idle handoff; do not infer readiness from a mock.
- 2026-09-24 after PR21: [PR #21](https://github.com/ProfessorBagholder/AgentWay/pull/21) merged at `1c6e318` after both CI runs passed. The existing test instance was rebuilt from merged `main` and its local health plus public non-browser agent ingress checks passed. Muse's supplied run history attributes the live task's wake to the new SSE subscriber and hook; AgentWay independently recorded its claim, completion and active Codex receipt. This is one receiver path, not product-wide automatic delivery. [PR #20](https://github.com/ProfessorBagholder/AgentWay/pull/20) contains the separate synthetic automatic-transport conformance harness; resolve its conflict with merged main, rerun its checks and present it for review. Do not infer a verified receiver binding from SSE connectivity or synthetic proof. The next implementation slice after that PR is bounded, observable delivery attempts with restart and uncertain-outcome tests.
- 2026-09-24 after PR19: the owner approved [PR #19](https://github.com/ProfessorBagholder/AgentWay/pull/19); it merged into `main` at `9c61f6c`. The app container was rebuilt and explicitly recreated from that checkout. Local `/` and `/health/media` returned 200; the public `/v1/status` returned the expected unauthenticated 401 from a Python-style client. [PR #20](https://github.com/ProfessorBagholder/AgentWay/pull/20), branch `feat/handoff-receiver-conformance` at `cdb44ed`, adds an isolated HTTP transport test and CI coverage. It checks that unverified bindings fail closed, then synthetically verifies disposable bindings **only in its temporary database** and passes offer → admission → server restart → agent claim/complete → return admission → sender receipt and grant-revocation checks. The new harness, older companion demo, Python compilation and diff checks passed locally; PR CI was pending at this checkpoint. This is not native wake or a production verification route. **Next action:** implement and test durable bounded delivery attempts against a fake adapter, including restart and uncertain outcomes; keep automatic delivery unavailable to normal users until product-native two-way proof and parity exist.
- 2026-09-24 webhook feasibility update: official [Grok Bot routine help](https://prod.cursor.com/help/grok-bot/routines) confirms a per-routine POST URL and bearer key; HTTP 200 starts a Bot run but is not completion. A single live POST from `prototypes/grok_webhook_probe` returned HTTP 200 and task `ccd0ac5b-98c5-45c4-b938-33def097b0a1` was then authenticated as claimed and completed by the intended Grok connection. The saved result omitted the final period requested in the routine. The temporary Codex → Grok grant was removed. This establishes one-shot webhook ingress and task execution, not durable AgentWay delivery, unattended sender return, failure recovery, or product-wide parity. The next phase is to design a secure owner-enrolled binding and durable event dispatch, then test the full bidirectional conformance scenario without user credential entry or relay. Keep automatic mode disabled meanwhile. Muse's reported VM cron ability is not proof of waking Muse itself; its pull inbox remains the verified mode.
- 2026-09-24 Muse receiver probe: Codex queued one 30-minute task `af73f3c6-fc78-496c-af0d-a847dc5700ad` after the owner authorized a temporary Codex → Muse grant. AgentWay records Muse claiming it 31 seconds later and completing it eight seconds after that with the exact requested result. The grant was removed. The owner relayed Muse's run history: a manual check at 10:32:39 MT found nothing before task creation; a distinct run at 10:37:36 MT had `trigger_reason: scheduled`, completed the task, and disabled itself. This is one unattended scheduled agent pickup, corroborated by AgentWay's task audit; the native run record was not independently inspected here. It is timer-based polling, not AgentWay event-triggered wake or proof of idle sender return.
- Repo: `/Users/md/dev/AgentWay`; PR19 is merged. Check `git status --short --branch`, `git log -1 --oneline`, and PR state before editing; do not trust a saved commit hash over Git.
- Earlier test deployment at `50f3796` confirmed guidance 18, pull mode and zero native wake. The main rebuild after PR19 retained that contract. Migration `0021` previously preserved six pull tasks and zero outbox entries; no new database-integrity check was run during the main rebuild. Live authenticated agent API verification of that test deployment was not performed because automatic security review rejected sending a stored connection credential to the public endpoint. The supported mode remains the permissioned pull inbox; automatic native delivery is unavailable.
- Real evidence: Grok Bot created task `80774cfa-1d40-444d-bd95-445ba7633d51` for Codex, which required user ID relay. A later task `8387e338-6d2b-4272-b1e6-2924eaaaca78` was created by Grok Bot while this Codex task was idle; its same-thread two-minute heartbeat discovered, claimed and completed it without ID relay. Grok Bot was active and polling, and read the result. This proves scheduled same-conversation Codex pickup, not event-triggered wake or idle Grok return delivery. A separate Codex app-server could not resume this desktop conversation while its active writer was held.
- Guidance 18 live acceptance: Grok Bot created pull task `d4ff094c-c67e-4f7d-ac57-872dff8f28a5` with a 30-minute deadline. The owner screen showed “Awaiting pickup.” On a later user-initiated turn in this existing Codex conversation, Codex found it through the incoming inbox without an ID relay, verified its identity and instructions, claimed and completed it. On a later active turn, Grok Bot read and acknowledged the exact result. Authenticated AgentWay readback confirmed `completed`, result `Codex received the test.`, and receipt timestamp `2026-09-24T15:21:40.410Z`. No native wake, unattended sender return or publishing was tested or claimed. See `docs/STATUS.md` for the acceptance record.
- Muse live acceptance: Codex queued pull task `97fcbeb6-cde4-4b6b-9173-0c030f4f0035` for Muse with a temporary owner grant and a 30-minute deadline. On an active turn Muse refreshed guidance 18, found, claimed and completed it with `Muse received the test.` Codex read the result and acknowledged through the local authenticated agent listener; readback confirmed the exact result and receipt timestamp `2026-09-24T15:29:29.974Z`. The temporary Codex → Muse grant was removed. This proves active-turn Muse participation and sender readback, not native wake or unattended return. The existing Grok → Codex grant remains unchanged. No publishing occurred.
- Current phase: **build the dormant automatic-delivery foundation with isolated conformance tests**. The merged guidance 18 pull inbox remains the only supported product mode. The [delivery foundation](handoff-delivery-foundation.md) fails automatic opt-in closed because no native binding can be verified. The [isolated receiver companion](../../prototypes/handoff_receiver/README.md) proves synthetic two-way pull round trips; the transport test exercises synthetic automatic records through the real HTTP listener and a server restart. Live scheduled Codex, Grok and Muse pickup each passed once; a later idle-to-idle Grok retry missed two intervals. Pull deadlines do **not** stop external routines or deliver to an idle sender. Native adapters remain a separate, gated phase.
- Deadline slice validated 2026-09-24 UTC: migration preserved legacy cursor/receipt values; six handoff unit tests, 50 Rust unit tests, four API tests, strict Clippy, web build and 16 mocked browser tests passed. The timeout detail was visually checked at 390/900/1440px in light/dark. An isolated synthetic two-way receiver demo still passed. The live test deployment returned guidance 17 with the timeout schema, and the public unauthenticated agent endpoint returned 401. No new task or native agent timeout was exercised on the user's live connections. Task expiry does not stop external product routines, and there is no idle sender return path.
- Delivery foundation validation 2026-09-24 UTC: migration `0021` preserves a legacy task rowid, revision and pull mode; tests cover two-binding eligibility, request-ID mode conflict, atomic offer/return creation, outbox rollback, timeout return, secret rotation, transport scope/admission, delivery-ID-bound native claims and sender receipts, grant revocation, finite outbox expiry and route credential isolation. Rust formatting, strict Clippy, 57 unit tests, four API tests, and web formatting/build passed. The test deployment retained all six pull tasks, zero outbox rows and database integrity; the public unauthenticated receiver feed returned 401. This is storage and transport evidence only; no native adapter or live receiver proof is claimed.
- Core dependency: supported unattended delivery **into** the intended native agent/session and **back** to the original sender. Never substitute a new model/API session, browser automation, an unsupported app-server writer, a one-off poll, or user relay. Never put tokens, native session IDs or real user task text in Git, test fixtures, PR text or chat.

## Product decision (2026-09-24)

The owner chose a graceful common pull-inbox contract rather than blocking all handoffs on universal native wake. Every connected agent receives the same AgentWay API and guidance. A task is stored for the recipient and read on that agent's next check; the result is stored for the sender and read on that sender's next check. Neither check is guaranteed to happen while its agent is idle. Agents must tell users this before assignment, use finite deadlines, avoid indefinite polling, and only offer a product-native background routine with the user's approval and a finite stop. Automatic delivery is a distinct future capability and must stay unavailable until product-wide native proof exists. This decision supersedes earlier release-gate language that required native wake before **any** handoff recipient could be shown. It does not make the failed idle-to-idle probe a success.

After each phase update this checkpoint with the date, actual branch/PR and commit, completed steps, automated checks, **live native evidence**, blocker, and single next action. Update `docs/STATUS.md` separately for implemented facts. If an experiment disproves an assumption, correct the architecture and plan before proceeding. An agent's self-report or a mock test alone cannot close a live gate.

## Reverse probe result (2026-09-23)

The owner created an enabled five-minute routine in the existing Grok Bot conversation. Codex created task `277785fe-44a6-48b7-851f-2ba5f6d67164` at 23:30:15 UTC with the exact title and instructions below. Codex's two-minute sender checks ran at 23:32, 23:34 and 23:36 UTC, found the task queued, and stayed silent, although the desktop displayed scheduled-task entries. Grok Bot later reported its routine status was **never run**. Only after the owner's 23:38 UTC message did Grok Bot manually claim and complete the task (23:39:39 UTC) in its existing conversation. Codex read the exact result and recorded the sender acknowledgment at 23:40:06 UTC. This proves manual recipient pull and live sender receipt, **not** unattended Grok pickup or an idle round trip. No publishing occurred. The Grok routine and Codex sender heartbeat were paused; the temporary Codex → Grok grant was revoked. The task remains completed and auditable. Grok Bot subsequently reported that its routine appeared in the catalog, but this Bot had no local `automations/.../automation.json` job file and no completion-inbox wake; it could not identify why job creation failed. Treat those as Grok's diagnostic report, not independently verified AgentWay evidence. Inspect Grok's native routine creation/run logs and verify a runnable job exists before a new no-prompt test.

The owner then asked Grok Bot to recreate the routine. Grok reported an enabled five-minute job file and showed a native routine event. Codex created a **new** task `ce69ec4d-0646-4df7-88fc-2a03a0adc65a` at 23:45:55 UTC without relaying its ID or prompting Grok afterward. AgentWay records the Grok connection claiming it at 23:50:50 UTC and completing it with the exact result at 23:51:00 UTC. The existing Grok conversation displayed the matching task ID and a disabled-routine event (the routine intentionally paused after one match). Codex read and acknowledged the result at 23:51:45 UTC. This is a successful scheduled Grok pickup and active Codex sender readback for one harmless task, after the first failed setup. It does **not** establish idle sender wake, reliability across restarts/failures, or parity across all connected products. No publishing occurred; the temporary Codex → Grok grant was revoked again. Grok's stated 5:49 routine start is not the AgentWay claim timestamp; use the recorded 23:50:50 claim for pickup latency (~4m55s).

Idle-to-idle retry (2026-09-24 UTC): Codex resumed Grok's five-minute routine, created task `d9b6ecc0-ace6-4579-8c93-3078e2f4a648` at 01:31:44 UTC, then ended its turn. The Codex sender heartbeat checked at 01:34, 01:36, 01:38 and 01:40 UTC; AgentWay still showed `queued` with no claim. At 01:44:20 UTC, after more than two Grok intervals, Codex cancelled the task, paused its own heartbeat and revoked the temporary Codex → Grok grant. Codex then paused Grok's routine in its native UI and verified the control changed to Resume. This test **failed**; no idle sender result or acknowledgment occurred. Its original heartbeat had no timeout, so absent owner intervention it could have polled indefinitely. Future probes require an explicit deadline and automatic cleanup of both routines, task and grant even when pickup never happens. Preserve the earlier one-off success as partial evidence, not a reliability claim.

### Original probe procedure

The only external setup needed next is in the owner's **existing Grok Bot conversation**. Per [Grok Bot's routine documentation](https://docs.x.ai/grok-bot/skills-routines-and-automations), ask that Bot to create a recurring routine at its shortest supported interval in that same conversation. The routine must use the existing AgentWay connection to page through queued tasks and process **only** one task addressed to that Grok connection with title `AgentWay reverse receiver probe` and exact instructions `Confirm receipt. Do not publish anything.` It must claim with a newly generated UUID v4, complete with `Grok received the handoff in the existing conversation. No publishing action was taken.`, report the task ID in the same conversation, and pause itself. It must ignore all other task content, publish nothing, and not retry a failed claim with a new token. The owner should verify the routine's next run and that it belongs to the same Bot; if the product cannot create such a routine, record that outcome rather than inventing a new session.

Only after the routine is visible, enable the narrow Codex → Grok directed grant on the test branch. Then Codex creates exactly one harmless task with those exact fields; a same-thread Codex sender check waits for terminal status, reads the result and calls the new `acknowledge_agent_task_result` only after this conversation receives it. Record both native transcripts, task history, `result_acknowledged_at`, pickup/return latency and any routine failure. Do not paste task IDs between agents. Pause/revoke temporary test setup afterward. This is a **feasibility probe**; passing it still does not satisfy busy/offline/restart cases or the all-product release gate.

## Product contract

An authorized sender assigns a permitted, independently connected agent. AgentWay records the task durably; that intended idle agent receives it without human relay, accepts or rejects it, acts under its **own** grants, and returns a result or actionable failure. The original sender receives the result without human relay. Connection authentication, receiver admission, agent acceptance, execution, result commit and result delivery are distinct facts. All advertised recipients share the same user-visible states, deadlines, diagnostics and recovery behavior; native transports may vary. A mere queued task is never called delivered.

## Phases and exit gates

### 0. Define the universal contract and prove native feasibility

First freeze the [common receiver contract](handoff-receiver-contract.md) for every agent: versioned register/bind, receive/fetch, accept/reject, claim/renew, progress, result, result receipt, cancellation, deadline and recovery; one authorization model, task state machine and conformance suite. Define how the common envelope reaches an idle native agent and how that agent proves it accepted work. Do not encode a particular vendor's routine or message API into the core contract. Then inspect official APIs, plugins and native automation for every advertised agent product: Muse, Grok Bot, Codex, Claude and ChatGPT. Record exact product/version, supported ingress, whether it targets the existing agent/session, authentication/owner binding, idle wake, return delivery, cancellation, liveness, limits and vendor restrictions. Use `verified`, `documented but untested`, `unknown`, or `unsupported`; do not infer a product API from its model API, MCP client, or internal agent-to-agent messaging.

Run the smallest isolated harmless probe for each plausible route using the **same** conformance scenario: idle target receives a task, authenticates back to AgentWay, acknowledges, and returns a harmless result. A product-native recurring routine may qualify only if it is durable, unattended, same-agent, within a measured pickup deadline, and reports failure. No publishing or user-content changes in feasibility probes. Record evidence in a receiver compatibility matrix and update [the delivery gate](handoff-delivery-gate.md). **Exit:** the common contract is frozen and each product in the proposed handoff scope has a credible supported path; two real products have reproducible ingress and return as an initial cross-product proof. The full bidirectional flow and remaining products are proved in Phase 3. If the full proposed set cannot meet the contract, surface that scope decision to the owner instead of silently accepting feature disparity or building speculative product-specific infrastructure.

### 1. Implement the common contract and add state safely

Implement the frozen versioned receiver register, dispatch, receipt, observation, result and cancel schemas with typed errors, binding generation and deadlines. Keep task execution state separate from outbound and return-delivery state. Add an **additive** migration after `migrations/0018_agent_handoffs.sql` for binding, delivery outbox/attempt and receipt tables. Preserve every existing task, grant, credential, publication, media ID and request ID. Do not backfill old queued tasks with invented receipts. Commit task + audit event + outbox row in one SQLite transaction.

Add owner-approved target binding and agent-facing receipt APIs behind a disabled flag. Authority comes from the connection credential; a caller-supplied agent ID is never authority. Reject cross-connection binding, stale generation and revoked grants. **Exit tests:** old API compatibility and YouTube regressions, migration on copied old data, failed journal/outbox insert rollback, wrong principal, replayed pairing, stale target and grant revocation. No new UI is needed yet.

### 2. Implement durable delivery with a fake receiver harness

An async dispatcher scans due outbox work at startup and after writes. Bound concurrency, retry/backoff, deadlines and attempts; an in-memory wake signal only accelerates processing. Send a short task/delivery-ID envelope; the recipient fetches task content with its own credential. Store native run correlation and reconcile a lost response before retrying. `admitted` is not `accepted`; a provider HTTP 2xx means only what its docs guarantee. Add a second outbox for delivering the committed result to the original sender. Exhausted retries become visible recovery, never dropped work. At-least-once delivery plus deduplication is the target; do not promise exactly-once external execution.

Build a fake receiver to simulate delayed admission, no agent acceptance, duplicate/out-of-order messages, offline/restart, wrong target, rejected work, input required, cancellation, and lost result delivery. **Exit tests:** crash between task commit and dispatch; restart scans; duplicate/concurrent dispatch; lost admission; expired claim/lease; stale binding; recipient and sender offline; cancel/complete race; idempotent result return; no leaked secrets in errors/events. This harness proves AgentWay mechanics only, not native product support.

### 3. Prove every advertised product against one conformance suite

Implement the first product adapter using only the supported path proven in Phase 0. Pair the owner-approved target; verify idle delivery to the **exact existing native agent/session**, authenticated claim, completion, and return to the original sender. Inspect native transcript/run evidence, not just AgentWay status. Repeat after restart and revocation. Add the second real product and test **both directions**, idle-to-idle without user relay. If a provider only starts a new assistant or cannot return results to the sender's existing session, do not call the adapter complete.

Record per-product route/version, private native correlation, binding evidence, measured pickup and return latency, and failures. Recheck after client or endpoint changes. **Exit:** an initial pair passes bidirectional interoperability, **and every product offered in the handoff feature** passes the same suite, including busy-session, offline/restart, duplicate, revoked-grant, cancellation and lost-response cases. Every reported acceptance/delivery has native receipt or visible transcript evidence. Do not extrapolate one product's pass to another.

### 4. Expose verified capability and finish the UX

Only a currently verified binding may appear in recipient discovery, receive a new directed grant or accept `create_agent_task` as automatic handoff. Existing pull-only tasks remain readable/recoverable. If the sender cannot receive an automatic return, reject or explicitly label that limitation **before** accepting work; never silently downgrade. Update agent guidance only when live behavior exists; preserve older MCP/HTTP clients and publishing tools.

Use [the UI system](ui-system.md). Keep Agents rows concise; put handoff readiness and a concrete recovery action in detail only when relevant. Tasks shows title, sender → recipient and truthful state. Activity log holds correlated attempts/errors; link to it from task detail rather than duplicate it. No new nav, placeholder tile, vague online badge or decorative explanation. **Exit checks:** 390/900/1440 px, light/dark, keyboard/focus/labels, no hidden fields, targeted SSE updates and actual recovery states. Browser fixtures are not proof of native delivery.

### 5. Security, operations and release

Review prompt-injection boundary (agent-supplied task text is untrusted), task/asset scopes, callback SSRF, auth in both directions, secret redaction, per-principal rate limits, loop/fan-out/concurrency limits, retention/backup and cancellation honesty. A recipient never inherits the sender's YouTube permission; guessed IDs and duplicate notifications cannot create access or double-publish. Run `AGENTS.md` Rust fmt, strict Clippy, workspace tests, web format/build and affected browser tests. Preserve verified Muse/Grok publishing, credentials, public URL and database through test deployment.

Deploy the **feature branch** as a labelled test build. Record live bidirectional acceptance over real ingress, restart and recovery, matching AgentWay UI/Activity log to native transcripts. Update `docs/STATUS.md`, evidence table and PR text with only verified claims. Keep the PR draft if return delivery or any advertised recipient fails parity. Merge **only** on the user's explicit approval, then switch to `main`, rebuild/restart from `main`, verify connections and public URL, and report the deployed commit. Never call an unmerged test build `main`.

## Implementation map and scope

- Foundation: `migrations/0018_agent_handoffs.sql`, `crates/server/src/publishing/handoffs.rs`, `mcp.rs`, `guidance.rs`, `connections.rs`, and `web/src/workspace.tsx`. Tests include handoff cases in `handoffs.rs` and `web/tests/workspace.spec.ts`. Inspect current paths/callers before editing.
- Preserve `/v1/agents`, `/v1/agent-tasks`, MCP tool names, recipient-generated claim UUID behavior, agent-scoped reads and loopback owner routes unless a versioned compatibility change is justified. Publishing/media behavior is a regression constraint, not an incidental refactor target.
- PR #19 merged the pull-inbox contract with explicit owner approval. Keep later automatic-delivery work in focused, reviewable branches. If scope grows beyond one coherent review, split dependent phases; do not present a partial automatic implementation as a complete handoff.
- No new platform connector, dashboard, speculative controls, unrelated restyle, or full Temporal/Redis runtime in this task without measured need. Reuse patterns and standards; check third-party licensing before copying code.

## Stop conditions

Stop a particular adapter if its native route is undocumented/unsupported, reaches a different agent/session, misses the pickup deadline, or cannot provide trustworthy receipt and return delivery. Keep the product's existing publishing connection; mark handoff unavailable, with an accurate reason. If any product in the proposed advertised scope cannot pass the common contract, do not release autonomous handoffs for a subset by default. Report the provider dependency, resolve scope with the owner, and continue only with independently useful foundation work.
