name: Checks
on: [push, pull_request]
permissions:
  contents: read
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: '1.98'
          components: rustfmt, clippy
      - uses: actions/setup-node@v4
        with:
          node-version: '22.14.0'
          cache: npm
          cache-dependency-path: web/package-lock.json
      - run: python3 tests/launcher/test_run.py
      - run: cargo fmt --check
      - run: cargo clippy --locked --workspace --all-targets -- -D warnings
      - run: cargo test --locked --workspace
      - run: cargo build --locked -p agentway-server
      - run: npm ci --prefix web && npm run format:check --prefix web && npm run build --prefix web
      - run: python3 tests/architecture/handoff_stream.py
      - run: ./run --no-open
      - run: cargo build --locked -p agentway-server && python3 prototypes/handoff_receiver/transport_conformance.py
      - run: python3 tests/architecture/resumable_integration.py
      - run: npx playwright install --with-deps chromium
        working-directory: web
      - run: npm run test:e2e
        working-directory: web
      - if: always()
        run: docker compose logs --tail 100
